function funcao1(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.tipoServicoSelecionado.value = aux;
}
function funcao2(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.seular.value = aux;
}
function funcao3(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.banheiros.value = aux;
}
function funcao4(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.quartos.value = aux;
}
function funcao5(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.janelas.value = aux;
}
function funcao6(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.geladeira.value = aux;
}
function funcao7(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.armario.value = aux;
}
function funcao8(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.aspirar.value = aux;
}
function funcao9(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.garagem.value = aux;
}
function funcao10(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.churrasqueira.value = aux;
}
function funcao11(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.agendamento.value = aux;
}
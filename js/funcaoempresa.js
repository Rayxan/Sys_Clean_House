function funcao1(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.locserv.value = aux;
}
function funcao2(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.banheiros.value = aux;
}
function funcao3(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.areatotal.value = aux;
}
function funcao4(Value)
{
    var aux = "";
    aux = Value;
    // alert(aux);
    // alert(document.all.tipoServicoSelecionado.value);
    document.all.agendamento.value = aux;
}